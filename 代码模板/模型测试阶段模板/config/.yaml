DATA:
    IMAGE_SIZE: 224
    CROP_PCT: 1.0

MODEL:
    #你需要做的是修改MODEL里的参数，根据不同的数据集和网络结构
    #调整MODEL的具体参数，
    #下面的为cvt的实例，具体使用需要全部删除写成具体你要实现<model>。
    #所以，你需要改动下面的部分。
    NAME: CvT_13
    NUM_CLASSES: 1000
    NUM_STAGES: 3
    PATCH_SIZE: [7, 3, 3]
    PATCH_STRIDE: [4, 2, 2]
    PATCH_PADDING: [2, 1, 1]
    DIM_EMBED: [64, 192, 384]
    NUM_HEADS: [1, 3, 6]
    DEPTH: [1, 2, 10]
    ATTN_DROP_RATE: [0.0, 0.0, 0.0]
    DROP_RATE: [0.0, 0.0, 0.0]
    DROP_PATH_RATE: [0.0, 0.0, 0.1]
    CLS_TOKEN: [False, False, True]